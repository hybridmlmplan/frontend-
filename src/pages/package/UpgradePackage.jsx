import React from "react";
export default function UpgradePackage(){ return <div className="p-4 bg-white rounded shadow">Upgrade package flow — use BuyPackage component to select higher package and pay/epin.</div>; }
